/* Viewport overlay: toolbars plus retained status dialogs.
 * Shared widget styles are in components.rcss (auto-included via applyTheme). */

body {
    font-family: Inter;
    width: 100%;
    height: 100%;
    position: relative;
}

#dm-root {
    position: relative;
    width: 100%;
    height: 100%;
}

#viewport-content {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    overflow: hidden;
}

.viewport-split-divider {
    position: absolute;
    pointer-events: none;
    z-index: 1;
}

.viewport-frame-border {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 4dp;
    pointer-events: none;
    z-index: 1;
}

.left-dock-resize-indicator {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    pointer-events: none;
    z-index: 12;
}

/* Centered in the viewport; the side padding keeps the wrapped lines clear of
   the docked tool rail. */
.empty-state-hint {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
    padding: 0 72dp;
    text-align: center;
    pointer-events: none;
}

/* viewport-cramped is set from the viewport size: the hint shows whole or not at all. */
#viewport-content.viewport-cramped .empty-state-hint {
    display: none;
}

.empty-state-icon {
    width: 48dp;
    height: 48dp;
    margin-bottom: 16dp;
}

.empty-state-title {
    font-size: 15dp;
}

.empty-state-subtitle {
    max-width: 480dp;
    margin-top: 10dp;
    font-size: 13dp;
}

.empty-state-import {
    margin-top: 10dp;
    font-size: 12dp;
}

.project-drop-overlay {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
    border-width: 2dp;
    border-radius: 8dp;
    pointer-events: none;
    z-index: 30;
}

.project-drop-overlay.hidden {
    display: none;
}

.project-drop-card {
    max-width: 420dp;
    padding: 14dp 20dp;
    border-width: 1dp;
    border-radius: 8dp;
    text-align: center;
}

.project-drop-title {
    font-size: 17dp;
    font-weight: 600;
}

.project-drop-label {
    margin-top: 5dp;
    font-size: 13dp;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.panel-toolbar-root {
    position: absolute;
    top: 0;
    height: 100%;
    display: flex;
    align-items: center;
    pointer-events: none;
}

.panel-toolbar-root.toolbar-position-top,
.panel-toolbar-root.toolbar-position-free {
    align-items: flex-start;
}

.toolbar-container {
    display: flex;
    padding: 0;
    gap: 0;
}

.toolbar-vertical {
    position: relative;
    left: 0;
    width: 38dp;
    height: auto;
    padding: 5dp 3dp;
    flex-direction: column;
    align-items: center;
    pointer-events: auto;
    overflow: hidden;
}

.toolbar-drag-handle {
    display: none;
    width: 30dp;
    height: 12dp;
    margin: 0 0 2dp 0;
    flex-shrink: 0;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2dp;
    cursor: move;
    drag: drag;
    pointer-events: auto;
}

.toolbar-position-free .toolbar-drag-handle {
    display: flex;
}

.toolbar-drag-line {
    display: block;
    width: 13dp;
    height: 1dp;
}

.toolbar-vertical .icon-btn {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 30dp;
    height: 30dp;
    min-width: 30dp;
    min-height: 30dp;
    margin: 1dp 0;
    padding: 3dp;
    border-width: 1dp;
    border-color: transparent;
    border-radius: 6dp;
    box-sizing: border-box;
    flex-shrink: 0;
    transition: background-color border-color 0.12s quadratic-out;
}

.toolbar-vertical .icon-btn.selected {
    padding: 3dp;
    border-width: 1dp;
}

.toolbar-vertical .icon-btn img {
    width: 20dp;
    height: 20dp;
}

.toolbar-vertical .rail-columns,
.toolbar-vertical .rail-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    flex-shrink: 0;
}

.toolbar-vertical.rail-tools-empty .rail-group--tools,
.toolbar-vertical.rail-panels-empty .rail-group--panels,
.toolbar-vertical.rail-tools-empty .rail-columns > .toolbar-separator,
.toolbar-vertical.rail-panels-empty .rail-columns > .toolbar-separator {
    display: none;
}

.toolbar-vertical.rail-two-column {
    width: auto;
}

.toolbar-vertical.rail-two-column .rail-columns {
    flex-direction: row;
    align-items: stretch;
}

.toolbar-vertical.rail-two-column .rail-columns > .toolbar-separator {
    width: 1dp;
    height: auto;
    margin: 0 5dp;
}

.toolbar-vertical.rail-compact .icon-btn {
    width: 26dp;
    height: 26dp;
    min-width: 26dp;
    min-height: 26dp;
    margin: 0;
    padding: 2dp;
}

.toolbar-vertical.rail-compact .icon-btn img {
    width: 18dp;
    height: 18dp;
}

.toolbar-vertical.rail-compact .rail-columns > .toolbar-separator {
    margin-top: 4dp;
    margin-bottom: 3dp;
}

.viewport-gizmo-controls {
    position: absolute;
    top: 108dp;
    right: 10dp;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 95dp;
    gap: 3dp;
    pointer-events: none;
    z-index: 8;
}

.viewport-gizmo-control-row {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    width: 95dp;
    gap: 3dp;
    pointer-events: auto;
}

.viewport-gizmo-controls .icon-btn {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 27dp;
    height: 27dp;
    min-width: 27dp;
    min-height: 27dp;
    margin: 0;
    padding: 3dp;
    border-width: 1dp;
    border-color: transparent;
    border-radius: 5dp;
    box-sizing: border-box;
    flex-shrink: 0;
    opacity: 0.88;
    transition: background-color border-color opacity 0.12s quadratic-out;
}

.viewport-gizmo-controls .icon-btn:hover,
.viewport-gizmo-controls .icon-btn.selected {
    opacity: 1;
}

.viewport-gizmo-controls .icon-btn img {
    width: 18dp;
    height: 18dp;
}

.toolbar-hcenter {
    position: absolute;
    left: 0;
    right: 0;
    display: flex;
    justify-content: center;
    pointer-events: none;
}

.toolbar-hcenter > div {
    pointer-events: auto;
}

.toolbar-hcenter .toolbar-container {
    align-items: center;
    padding: 5dp 6dp;
    border-radius: 6dp;
    box-sizing: border-box;
}

.toolbar-hcenter .icon-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 30dp;
    height: 30dp;
    min-width: 30dp;
    min-height: 30dp;
    padding: 3dp;
    border-width: 1dp;
    border-color: transparent;
    border-radius: 6dp;
    box-sizing: border-box;
    flex-shrink: 0;
    transition: background-color border-color 0.12s quadratic-out;
}

.toolbar-hcenter .icon-btn img {
    width: 20dp;
    height: 20dp;
}

.toolbar-layer-primary {
    top: 5dp;
}

.crop-roi-popover {
    position: absolute;
    top: 42dp;
    left: 50%;
    width: 440dp;
    margin-left: -220dp;
    padding: 8dp 10dp;
    display: flex;
    flex-direction: column;
    gap: 3dp;
    border-width: 1dp;
    border-radius: 6dp;
    box-sizing: border-box;
    pointer-events: auto;
    z-index: 10;
}

.crop-roi-popover.hidden {
    display: none;
}

.crop-roi-popover-title {
    height: 20dp;
    line-height: 20dp;
    font-size: 12dp;
    font-weight: bold;
}

.crop-roi-setting-row > .setting-row__label-col {
    width: 180dp;
    min-width: 180dp;
    max-width: 180dp;
}

.crop-roi-setting-row .setting-slider {
    height: 22dp;
    min-height: 22dp;
}

.crop-roi-setting-row .slider-value {
    width: 42dp;
    min-width: 42dp;
}

.viewport-selection-overlay {
    position: absolute;
    top: 5dp;
    left: 12dp;
    right: 12dp;
    display: flex;
    justify-content: center;
    pointer-events: none;
    z-index: 9;
}

.viewport-depth-overlay {
    position: absolute;
    top: 5dp;
    left: 12dp;
    right: 12dp;
    display: flex;
    justify-content: center;
    pointer-events: none;
    z-index: 9;
}

.viewport-transform-overlay {
    position: absolute;
    top: 5dp;
    left: 12dp;
    right: 12dp;
    display: flex;
    justify-content: center;
    pointer-events: none;
    z-index: 9;
}

.viewport-depth-header {
    flex-wrap: nowrap;
    align-items: center;
}

.viewport-transform-meta.viewport-depth-meta {
    min-width: 0;
    max-width: none;
    flex-direction: row;
    align-items: center;
    gap: 6dp;
    flex-grow: 0;
    flex-shrink: 0;
    min-height: 24dp;
}

.viewport-transform-meta.viewport-depth-meta .viewport-transform-meta__tool {
    line-height: 24dp;
    font-size: 12dp;
    white-space: nowrap;
}

#depth-view-block .viewport-transform-meta.viewport-depth-meta {
    min-height: 22dp;
}

#depth-view-block .viewport-transform-meta.viewport-depth-meta .viewport-transform-meta__tool {
    line-height: 22dp;
}

.viewport-depth-mode-select {
    width: 112dp;
    min-width: 112dp;
    height: 24dp;
    min-height: 24dp;
    font-size: 12dp;
    line-height: 22dp;
    padding: 0;
    margin: 0;
    flex-shrink: 0;
}

#depth-view-block .viewport-depth-mode-select {
    height: 22dp;
    min-height: 22dp;
    line-height: 20dp;
}

.viewport-depth-mode-select selectvalue {
    height: 22dp;
    line-height: 22dp;
    margin-right: 20dp;
    padding: 0 0 0 8dp;
    position: relative;
    top: 0;
}

#depth-view-block .viewport-depth-mode-select selectvalue {
    height: 20dp;
    line-height: 20dp;
}

.viewport-depth-mode-select selectarrow {
    width: 20dp;
    height: 22dp;
    line-height: 22dp;
    position: relative;
    top: 0;
}

#depth-view-block .viewport-depth-mode-select selectarrow {
    height: 20dp;
    line-height: 20dp;
}

.viewport-selection-depth-fields {
    min-width: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 6dp;
    flex-grow: 1;
}

#selection-block .viewport-selection-depth-fields {
    flex-wrap: nowrap;
    gap: 10dp;
    flex-grow: 0;
    flex-shrink: 1;
    position: relative;
}

/* Vertically center the 4dp track (and its progress fill) inside the 22dp
   slider box so it lines up with the label and value box; the thumb keeps
   its -3dp offset relative to the track line. */
#selection-block .viewport-selection-slider slidertrack {
    margin-top: 8dp;
}

#selection-block .viewport-selection-slider sliderbar {
    margin-top: 5dp;
}

#selection-block .viewport-selection-depth-axis {
    gap: 6dp;
}

/* Sub-rows: Near/Far and Size/X/Y ride side by side on one line at full
   width and stack as whole units when the row wraps. */
#selection-block .depth-axis-group {
    display: flex;
    align-items: center;
    gap: 10dp;
    min-width: 0;
    flex-grow: 1;
    flex-shrink: 1;
}

/* Depth toolbar width tiers:
   >1480dp: Near/Far and Size/X/Y on one line.
   >960dp through 1480dp: Near/Far above Size/X/Y.
   <=960dp: icons only; depth fields hidden. No taller stack.
   Breakpoints derive from the effective cascade; see calculations above each @media. */
#selection-block .viewport-selection-depth-axis {
    flex-grow: 1;
    flex-shrink: 1;
    max-width: 330dp;
}

#selection-block .viewport-selection-depth-fields {
    flex-grow: 1;
}

#selection-block .viewport-selection-slider {
    min-width: 56dp;
    max-width: 240dp;
    flex-grow: 1;
    flex-shrink: 1;
}

#selection-block .viewport-selection-depth-axis > .number-input.viewport-selection-depth-input {
    /* Content-fit, never grows: the global .number-input flex: 1 would split
       the axis's spare width with the slider. RmlUi cannot size a text input
       to its content, so a tight fixed width stands in for it. */
    flex-grow: 0;
    flex-shrink: 0;
    width: 50dp;
    min-width: 50dp;
}

/* The chip labels the depth sliders' target panel in independent-dual only.
   Keep it inside Near/Far to avoid an extra wrapped line with the sync toggle.
   Keep this selector a descendant match so styling applies at every width. */
#selection-block .viewport-selection-depth-panel-chip {
    flex-grow: 0;
    flex-shrink: 0;
    width: 20dp;
    height: 20dp;
    line-height: 20dp;
    text-align: center;
    font-size: 12dp;
    font-weight: bold;
    border-radius: 4dp;
    /* Palette fill belongs in viewport_overlay.theme.rcss; see
       docs/docs/development/rmlui-styling.md, Theme Tokens. */
}

#selection-block .depth-axis-size > .number-input.viewport-selection-depth-input {
    /* Wide enough for the anisotropic "63x45%" Size readout at 1.5x UI scale. */
    width: 104dp;
    min-width: 104dp;
}

/* Wrap uses context width M (viewport + left dock), in every split mode.
   RmlUi media queries use context dimensions; top-level #selection-block has
   left 52dp/right 12dp, so its panel content width is A(M) = .65 * (M - 64).

   One-line minimum, with in-flow buttons (all dimensions in dp):
     Near/Far/X/Y axis = label 24 + gap 6 + slider 56 + gap 6 + input 50 = 142
     Size axis        = 24 + 6 + 56 + 6 + input 104                    = 196
     Near/Far group   = chip 20 + 10 + 142 + 10 + 142                  = 324
     Size/X/Y group   = 196 + 10 + 142 + 10 + 142                      = 500
     Full row         = 324 + 10 + 500 + 10 + button 30 + 10 + 30      = 914

   Fit requires M >= 914/.65 + 64 = 1470.154; 1471dp is the first whole dp.
   Wrap at 1480dp gives A=920.4dp, with 6.4dp spare. Without chip and sync
   (Primary/Secondary comparison), the 844dp row needs M>=1362.462, so this
   breakpoint covers it too. */
@media (max-width: 1480dp) {
    /* Center Near/Far above Size/X/Y with equal slider lengths.
       Pin one action button to the right of each line; this RmlUi has no flex
       `order`, so flow would place both on the last line.
       Sync shares the chip's independent-dual visibility and sits beside Near/Far.
       Viz stays beside Size/X/Y whenever the depth row is shown. */
    #selection-block .viewport-selection-depth-fields {
        flex-wrap: wrap;
        /* Reserve 36dp on the right for one 30dp button plus 6dp clearance.
           Mirror it on the left to keep both lines centered. Buttons use the padding
           box (right: 0), while flex-basis:100% uses the content box. Include both
           reserves (72dp total) when calculating line width and the hide breakpoint. */
        padding-right: 36dp;
        padding-left: 36dp;
    }
    #selection-block .depth-axis-group {
        flex-basis: 100%;
        justify-content: center;
    }
    #selection-block .depth-axis-group .viewport-selection-slider {
        width: 90dp;
        min-width: 56dp;
        max-width: 150dp;
    }
    #selection-block .viewport-selection-depth-fields > .viewport-transform-action {
        position: absolute;
        right: 0;
    }
    /* Keep one button per line on the same right edge. Center each 30dp button
       on its 24dp line with -3dp outer offsets. The 58dp row (24 + 10 + 24)
       then has a 4dp button gap; flush top/bottom placement would overlap by 2dp.
       The 3dp overhang fits inside the 4dp upper gap and 5dp bottom padding,
       leaving row height and panel-bottom clearance unchanged. */
    #selection-block .viewport-selection-depth-fields > .viewport-selection-sync-action {
        top: -3dp;
    }
    #selection-block .viewport-selection-depth-fields > .viewport-selection-viz-action {
        bottom: -3dp;
    }
    /* With depth fields visible, the panel (z-index 9, pointer-events auto)
       can cover the viewport gizmos (z-index 8). The 72dp row reserve protects
       only its own buttons. A split divider can put primary gizmos at any x,
       so horizontal clearance cannot protect them.

       Panel bottom = top 5 + border 1 + padding 5 + icon row 32 + gap 4
                      + fields height + padding 5 + border 1 = 53 + fields.
       One line: max(axis 24, in-flow button 30) = 30; bottom 83, leaving 25dp
       below the stock gizmo top 108. Two lines: 24 + gap 10 + 24 = 58; bottom
       111, so top 120 leaves 9dp. Three lines cannot clear stock top 108.
       Absolute buttons add no row height; the lower button ends at 108,
       inside the panel. Move gizmos in this tier and restore top when fields
       hide. These width rules also move gizmos when depth/GT state hides fields. */
    .viewport-gizmo-controls {
        top: 120dp;
    }
}

/* Hide is the final tier: widths that cannot fit two lines hide the row.
   Size/X/Y is the binding line at 500dp. Two 36dp reserves leave
     L(M) = .65 * (M - 64) - 72.
   Each reserve is button 30 + clearance 6; the buttons occupy one line
   each, and matching left padding centers the groups.
   The exact fit is M = (500 + 72)/.65 + 64 = 944dp. At 960dp, L=510.4dp
   leaves 10.4dp spare. Sliders are already at their 56dp floor, so
   332 + 3*56 = 500dp cannot shrink further.

   Near/Far needs 324dp, fitting at M=(324+72)/.65+64=673.231dp. These
   minima use #selection-block's ID-qualified axis rules: gap 6dp and
   flex-shrink 1, overriding generic gap 3dp/shrink 0. The chosen 960dp
   clears both lines. Above 1480dp buttons return to flow; reserves no
   longer apply. */
@media (max-width: 960dp) {
    #selection-block .viewport-selection-depth-fields {
        display: none;
    }
    /* The wrap query also matches here, so restore the controls' top from 120dp
       to 108dp. With only the icon row, the panel bottom is
       5 + 1 + 5 + 32 + 5 + 1 = 49dp, leaving 59dp clearance. */
    .viewport-gizmo-controls {
        top: 108dp;
    }
}

.viewport-selection-depth-axis {
    display: flex;
    align-items: center;
    gap: 3dp;
    min-width: 0;
    flex-shrink: 0;
}

.viewport-selection-depth-axis > .viewport-transform-axis-label {
    width: 24dp;
    min-width: 24dp;
}

.viewport-selection-slider {
    width: 112dp;
    min-width: 112dp;
    height: 22dp;
    min-height: 22dp;
    margin-top: 0;
    position: relative;
    top: 0;
}

.viewport-selection-depth-axis > .number-input.viewport-selection-depth-input {
    /* Wide enough for the anisotropic "63x45%" Size readout at 1.5x UI scale
       (88dp still clipped the percent sign). */
    width: 104dp;
    min-width: 104dp;
    height: 24dp;
    min-height: 24dp;
    padding: 2dp 5dp;
    line-height: 18dp;
    font-size: 12dp;
    box-sizing: border-box;
}

.viewport-depth-fields {
    flex-grow: 0;
    flex-shrink: 0;
    flex-wrap: nowrap;
    gap: 8dp;
}

#depth-view-block .viewport-depth-fields {
    gap: 5dp;
}

.viewport-depth-axis {
    gap: 4dp;
    height: 24dp;
    min-height: 24dp;
}

#depth-view-block .viewport-depth-axis {
    height: 22dp;
    min-height: 22dp;
    gap: 3dp;
}

.viewport-depth-axis > .viewport-transform-axis-label {
    width: auto;
    min-width: 0;
    font-size: 12dp;
    line-height: 24dp;
    text-align: left;
}

#depth-view-block .viewport-depth-axis > .viewport-transform-axis-label {
    line-height: 22dp;
}

.viewport-selection-slider.viewport-depth-slider {
    width: 128dp;
    min-width: 128dp;
    height: 24dp;
    min-height: 24dp;
    top: 3dp;
}

#depth-view-block .viewport-selection-slider.viewport-depth-slider {
    height: 22dp;
    min-height: 22dp;
    top: 0;
}

#depth-view-block .viewport-depth-axis > .scrub-field {
    width: 112dp;
    min-width: 112dp;
    height: 22dp;
    min-height: 22dp;
}

#depth-view-block .viewport-depth-axis > .viewport-depth-step-btn {
    width: 18dp;
    min-width: 18dp;
    height: 22dp;
    min-height: 22dp;
    line-height: 20dp;
    padding: 0 0 1dp 0;
    margin-left: 0;
    font-size: 12dp;
    border-radius: 4dp;
    box-sizing: border-box;
    flex-shrink: 0;
}

#depth-view-block .viewport-depth-axis > .viewport-depth-step-btn:disabled {
    opacity: 0.45;
    cursor: default;
}

.viewport-selection-actions {
    margin-left: 0;
}

.viewport-depth-actions {
    margin-left: 0;
}

.viewport-transform-panel {
    pointer-events: auto;
    min-width: 360dp;
    max-width: 100%;
    padding: 7dp 8dp;
    border-width: 1dp;
    border-radius: 8dp;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    gap: 6dp;
}

.viewport-transform-panel.viewport-selection-panel {
    min-width: 600dp;
}

#selection-block .viewport-selection-panel {
    min-width: 0;
    /* Window-relative: the panel tracks the viewport width and the sliders
       grow into it (capped below), instead of collapsing to the icon row. */
    width: 65%;
    max-width: 1500dp;
    padding: 5dp 6dp;
    border-radius: 6dp;
    gap: 4dp;
}

.viewport-transform-panel.viewport-depth-panel {
    min-width: 624dp;
}

#depth-view-block .viewport-depth-panel {
    padding: 5dp 6dp;
    border-radius: 6dp;
    gap: 0;
    min-width: 0;
}

#gt-compare-mode-block .viewport-gt-compare-panel {
    padding: 5dp 6dp;
    border-radius: 6dp;
    gap: 0;
    min-width: 0;
}

#gt-compare-mode-block .viewport-gt-compare-mode-select {
    width: 120dp;
    min-width: 120dp;
    height: 22dp;
    min-height: 22dp;
    line-height: 20dp;
}

.viewport-gt-color-legend {
    width: 240dp;
    min-width: 240dp;
    margin-top: 4dp;
}

.viewport-gt-color-scale {
    width: 240dp;
    height: 10dp;
    box-sizing: border-box;
    display: flex;
    overflow: hidden;
    border-width: 1dp;
    border-color: #585b70;
    border-radius: 3dp;
}

.viewport-gt-color-scale__segment {
    display: block;
    width: 0;
    height: 8dp;
    flex-grow: 1;
    flex-shrink: 0;
}

.viewport-gt-color-scale__segment--loss-lowest {
    decorator: horizontal-gradient(#000000 #380578);
}

.viewport-gt-color-scale__segment--loss-low {
    decorator: horizontal-gradient(#380578 #b81f4a);
}

.viewport-gt-color-scale__segment--loss-medium {
    decorator: horizontal-gradient(#b81f4a #fca60a);
}

.viewport-gt-color-scale__segment--loss-high {
    decorator: horizontal-gradient(#fca60a #ffffbf);
}

.viewport-gt-color-scale__segment--depth-far {
    flex-grow: 20;
    decorator: horizontal-gradient(#0d0a26 #0f3280);
}

.viewport-gt-color-scale__segment--depth-far-mid {
    flex-grow: 23;
    decorator: horizontal-gradient(#0f3280 #0080a6);
}

.viewport-gt-color-scale__segment--depth-mid {
    flex-grow: 24;
    decorator: horizontal-gradient(#0080a6 #5cbb69);
}

.viewport-gt-color-scale__segment--depth-near-mid {
    flex-grow: 19;
    decorator: horizontal-gradient(#5cbb69 #f6d14d);
}

.viewport-gt-color-scale__segment--depth-near {
    flex-grow: 14;
    decorator: horizontal-gradient(#f6d14d #fb6e20);
}

.viewport-gt-color-scale__segment--depth-gray {
    decorator: horizontal-gradient(#000000 #ffffff);
}

.viewport-gt-color-labels {
    width: 240dp;
    margin-top: 2dp;
    display: flex;
    justify-content: space-between;
    color: #a6adc8;
    font-size: 10dp;
    line-height: 12dp;
}

.viewport-transform-panel.viewport-export-panel {
    width: 470dp;
    min-width: 470dp;
    max-width: 470dp;
    padding: 6dp 8dp;
    gap: 4dp;
    box-sizing: border-box;
}

.viewport-transform-panel.viewport-export-panel.has-custom {
    width: 624dp;
    min-width: 624dp;
    max-width: 624dp;
}

.viewport-transform-panel.viewport-export-panel.has-alpha {
    width: 532dp;
    min-width: 532dp;
    max-width: 532dp;
}

.viewport-transform-panel.viewport-export-panel.has-custom.has-alpha {
    width: 686dp;
    min-width: 686dp;
    max-width: 686dp;
}

#transform-block .viewport-transform-panel {
    min-width: 0;
    padding: 5dp 6dp;
    border-radius: 6dp;
    gap: 0;
}

.viewport-transform-row,
.viewport-selection-row {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 5dp;
    min-width: 0;
}

.viewport-transform-tool-group,
.viewport-selection-tool-group {
    display: flex;
    align-items: center;
    gap: 0;
    padding: 0;
    border-width: 1dp;
    border-radius: 5dp;
    flex-shrink: 0;
}

.viewport-export-overlay {
    flex-direction: column;
    align-items: center;
    gap: 5dp;
}

.viewport-transform-header {
    display: flex;
    align-items: center;
    gap: 8dp;
    min-width: 0;
}

.viewport-transform-meta {
    min-width: 180dp;
    max-width: none;
    display: flex;
    flex-direction: column;
    gap: 1dp;
    flex-grow: 1;
    flex-shrink: 1;
}

.viewport-transform-meta__tool {
    font-size: 12dp;
    font-weight: bold;
}
.viewport-transform-context {
    display: flex;
    align-items: center;
    gap: 4dp;
    flex-shrink: 0;
}

.viewport-transform-option-group {
    display: flex;
    align-items: center;
    gap: 0;
    padding: 0;
    border-width: 1dp;
    border-radius: 5dp;
}

.viewport-transform-option-group.hidden {
    display: none;
}

.viewport-transform-option {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24dp;
    height: 24dp;
    min-width: 24dp;
    min-height: 24dp;
    padding: 3dp;
    border-radius: 4dp;
    box-sizing: border-box;
}

.viewport-transform-option img {
    width: 18dp;
    height: 18dp;
}

#transform-block .viewport-transform-option,
#transform-block .viewport-transform-action,
#selection-block .viewport-transform-option,
#selection-block .viewport-transform-action,
#depth-view-block .viewport-transform-action {
    width: 30dp;
    height: 30dp;
    min-width: 30dp;
    min-height: 30dp;
    padding: 3dp;
}

#transform-block .viewport-transform-option img,
#transform-block .viewport-transform-action img,
#selection-block .viewport-transform-option img,
#selection-block .viewport-transform-action img,
#depth-view-block .viewport-transform-action img {
    width: 20dp;
    height: 20dp;
}

.icon-btn.disabled,
.icon-btn.disabled:hover,
.icon-btn.disabled:active {
    background-color: transparent;
    border-color: transparent;
}

.icon-btn.disabled img {
    image-color: rgba(255, 255, 255, 95);
}

.viewport-export-fields {
    display: flex;
    align-items: center;
    gap: 8dp;
    height: 26dp;
    min-height: 26dp;
    flex-shrink: 0;
    flex-wrap: nowrap;
}

.viewport-export-axis {
    display: flex;
    align-items: center;
    gap: 6dp;
    height: 26dp;
    min-height: 26dp;
    flex-shrink: 0;
}

.viewport-export-header {
    flex-wrap: nowrap;
    align-items: center;
    min-height: 26dp;
}

.viewport-transform-meta.viewport-export-meta {
    width: 171dp;
    min-width: 171dp;
    max-width: 171dp;
    flex-direction: row;
    align-items: center;
    gap: 6dp;
    flex-grow: 0;
    flex-shrink: 0;
    min-height: 26dp;
}

.viewport-transform-meta.viewport-export-meta .viewport-transform-meta__tool {
    font-size: 12dp;
    line-height: 26dp;
    white-space: nowrap;
}

.viewport-export-axis > .viewport-transform-axis-label {
    width: 64dp;
    min-width: 64dp;
    font-size: 12dp;
    line-height: 26dp;
    text-align: left;
    white-space: nowrap;
    flex-shrink: 0;
}

.viewport-export-resolution-select,
.viewport-export-format-select {
    height: 26dp;
    min-height: 26dp;
    font-size: 12dp;
    line-height: 24dp;
    padding: 0;
    margin: 0;
    flex-shrink: 0;
}

.viewport-export-resolution-select {
    width: 102dp;
    min-width: 102dp;
}

.viewport-export-format-select {
    width: 74dp;
    min-width: 74dp;
}

.viewport-export-resolution-select selectvalue,
.viewport-export-format-select selectvalue {
    height: 24dp;
    line-height: 24dp;
    margin-right: 20dp;
    padding: 0 0 0 8dp;
    position: relative;
    top: 0;
}

.viewport-export-resolution-select selectarrow,
.viewport-export-format-select selectarrow {
    width: 20dp;
    height: 24dp;
    line-height: 24dp;
    position: relative;
    top: 0;
}

.viewport-export-custom-size {
    display: flex;
    align-items: center;
    gap: 5dp;
    height: 26dp;
    min-height: 26dp;
    flex-shrink: 0;
}

.viewport-export-custom-size.hidden {
    display: none;
}

.viewport-export-size-input {
    width: 64dp;
    min-width: 64dp;
    height: 24dp;
    min-height: 24dp;
    font-size: 12dp;
    line-height: 16dp;
    padding: 3dp 6dp;
    flex-shrink: 0;
    text-align: right;
    box-sizing: border-box;
}

.viewport-export-size-separator {
    width: 8dp;
    min-width: 8dp;
    font-size: 12dp;
    line-height: 26dp;
    height: 26dp;
    text-align: center;
    flex-shrink: 0;
}

.viewport-export-transparency {
    display: flex;
    align-items: center;
    gap: 4dp;
    height: 26dp;
    min-height: 26dp;
    font-size: 12dp;
    white-space: nowrap;
    flex-shrink: 0;
}

.viewport-export-transparency.hidden {
    display: none;
}

.viewport-export-actions {
    height: 26dp;
    min-height: 26dp;
    flex-shrink: 0;
}

.viewport-export-button {
    width: 66dp;
    min-width: 66dp;
    height: 26dp;
    min-height: 26dp;
    padding: 0;
    font-size: 12dp;
    line-height: 24dp;
    box-sizing: border-box;
    flex-shrink: 0;
}

.viewport-export-status {
    pointer-events: none;
    max-width: 360dp;
    min-height: 22dp;
    padding: 4dp 10dp;
    border-width: 1dp;
    border-radius: 6dp;
    font-size: 11dp;
    line-height: 14dp;
    white-space: nowrap;
    opacity: 1;
}

.viewport-export-status.hidden {
    display: none;
}

.viewport-transform-fields {
    min-width: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 6dp;
}

.viewport-transform-group {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 6dp;
    min-width: 0;
}

.viewport-transform-axis {
    display: flex;
    align-items: center;
    gap: 2dp;
    min-width: 0;
    flex-shrink: 0;
}

#transform-block .viewport-transform-fields,
#transform-block .viewport-transform-group {
    flex-wrap: wrap;
    gap: 4dp;
}

#transform-block .viewport-transform-fields {
    justify-content: flex-start;
    flex-shrink: 1;
}

.viewport-transform-axis-label {
    width: 10dp;
    min-width: 10dp;
    font-size: 11dp;
    text-align: center;
}

.viewport-transform-axis > .number-input {
    width: 64dp;
    min-width: 64dp;
    height: 22dp;
    min-height: 22dp;
    padding: 2dp 5dp;
    line-height: 16dp;
    font-size: 12dp;
    box-sizing: border-box;
}

.viewport-transform-axis > .num-step-btn {
    width: 22dp;
    height: 22dp;
    min-width: 22dp;
    min-height: 22dp;
    padding: 0 0 1dp 0;
    line-height: 20dp;
    margin-left: 0;
    box-sizing: border-box;
}

#transform-block .viewport-transform-axis-label {
    width: 8dp;
    min-width: 8dp;
}

#transform-block .viewport-transform-axis > .number-input {
    width: 58dp;
    min-width: 58dp;
    height: 22dp;
    min-height: 22dp;
}

#transform-block .viewport-transform-axis > .num-step-btn {
    width: 20dp;
    min-width: 20dp;
}

.viewport-transform-axis--split {
    margin-left: 4dp;
    padding-left: 6dp;
    border-left-width: 1dp;
}

.viewport-transform-actions {
    display: flex;
    align-items: center;
    gap: 3dp;
    flex-shrink: 0;
}

.viewport-transform-action {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 24dp;
    height: 24dp;
    min-width: 24dp;
    min-height: 24dp;
    padding: 3dp;
    border-radius: 5dp;
    box-sizing: border-box;
}

.viewport-transform-action img {
    width: 18dp;
    height: 18dp;
}

.toolbar-separator {
    width: 1dp;
    margin: 2dp 0;
}

.toolbar-vertical .toolbar-separator {
    width: 24dp;
    height: 1dp;
    margin: 8dp 0 7dp;
}

.toolbar-hcenter .toolbar-separator {
    width: 1dp;
    height: 22dp;
    margin: 0 5dp;
}

.viewport-transfer-queue {
    position: absolute;
    right: 8dp;
    bottom: 8dp;
    width: 352dp;
    max-width: 90%;
    max-height: 65%;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    gap: 8dp;
    padding: 8dp;
    border-width: 1dp;
    border-radius: 8dp;
    pointer-events: auto;
    z-index: 22;
}

.viewport-transfer-header, .viewport-transfer-title, .viewport-transfer-status,
.viewport-transfer-actions, .viewport-transfer-footer {
    display: flex;
    align-items: center;
    gap: 8dp;
    flex-shrink: 0;
}
.viewport-transfer-heading { flex: 1; font-size: 13dp; font-weight: bold; }
.viewport-transfer-toggle { width: 24dp; min-width: 24dp; padding: 0; }
.viewport-transfer-summary { font-size: 11dp; flex-shrink: 0; }
.viewport-transfer-list { min-height: 0; max-height: 320dp; overflow-y: auto; }
.viewport-transfer-row { display: flex; flex-direction: column; gap: 8dp; padding: 8dp 0; border-top-width: 1dp; }
.viewport-transfer-title { font-size: 12dp; font-weight: bold; }
.viewport-transfer-name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.viewport-transfer-status { flex-wrap: wrap; justify-content: space-between; font-size: 11dp; }
.viewport-transfer-detail { font-size: 11dp; }
.viewport-transfer-progress { height: 8dp; flex-shrink: 0; width: 100%; border-radius: 2dp; overflow: hidden; }
.viewport-transfer-fill { height: 100%; }
.viewport-transfer-footer { padding-top: 8dp; border-top-width: 1dp; flex-wrap: wrap; }
.viewport-transfer-queue .btn { box-sizing: border-box; height: 24dp; min-height: 24dp; max-height: 24dp; padding: 0 8dp; line-height: 22dp; font-size: 11dp; white-space: nowrap; }
.viewport-transfer-queue .viewport-transfer-toggle { padding: 0; width: 24dp; text-align: center; }
.viewport-transfer-queue.hidden, .viewport-transfer-queue .hidden { display: none; }
@keyframes transfer-progress-pulse { from { opacity: 1; } to { opacity: 0.35; } }
.viewport-transfer-fill.is-indeterminate { animation: transfer-progress-pulse 0.8s quadratic-in-out infinite alternate; }

.viewport-metrics-overlay {
    position: absolute;
    pointer-events: none;
    z-index: 20;
}

.viewport-lod-stats-overlay {
    position: absolute;
    pointer-events: none;
    z-index: 18;
}

.viewport-lod-stats-card {
    width: 356dp;
    padding: 8dp 10dp;
    border-width: 1dp;
    border-radius: 8dp;
    display: flex;
    flex-direction: column;
    gap: 4dp;
    box-sizing: border-box;
}

.viewport-lod-stats-title {
    font-size: 12dp;
    font-weight: bold;
    line-height: 16dp;
    margin-bottom: 2dp;
}

.viewport-lod-stats-row {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 8dp;
    min-height: 14dp;
}

.viewport-lod-stats-label {
    width: 70dp;
    min-width: 70dp;
    font-size: 9dp;
    line-height: 14dp;
    white-space: nowrap;
}

.viewport-lod-stats-value {
    flex-grow: 1;
    min-width: 0;
    font-family: "JetBrains Mono";
    font-size: 9dp;
    line-height: 14dp;
    text-align: right;
    white-space: normal;
    word-break: break-word;
}

.viewport-metrics-card {
    min-width: 152dp;
    padding: 10dp 12dp;
    border-width: 1dp;
    border-radius: 8dp;
    display: flex;
    flex-direction: column;
    gap: 6dp;
}

.viewport-metrics-row {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 16dp;
}

.viewport-metrics-label,
.viewport-metrics-value {
    font-size: 12dp;
}

.viewport-metrics-value {
    font-size: 15dp;
    font-weight: bold;
}

.vram-hud-overlay {
    position: absolute;
    right: 16dp;
    top: 16dp;
    width: 620dp;
    height: 72%;
    pointer-events: auto;
    z-index: 21;
}

.vram-hud-card {
    position: relative;
    height: 100%;
    box-sizing: border-box;
    padding: 10dp 12dp;
    border-width: 1dp;
    border-radius: 8dp;
    display: flex;
    flex-direction: column;
    gap: 8dp;
}

.vram-hud-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    drag: drag;
    cursor: move;
}

.vram-hud-tabs {
    display: flex;
    gap: 0dp;
    border-bottom-width: 1dp;
}

.vram-hud-tab {
    flex: 1;
    height: 24dp;
    font-size: 11dp;
    text-align: center;
    cursor: pointer;
    border-width: 0dp;
    border-radius: 0dp;
}

.vram-hud-panel {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 6dp;
    overflow-y: auto;
}

.vram-hud-panel.hidden {
    display: none;
}

.vram-hud-counters-header {
    font-size: 11dp;
    font-weight: bold;
    padding-top: 4dp;
}

.perf-hud-tracking-off {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12dp;
    padding: 10dp;
    border-width: 1dp;
    border-radius: 5dp;
    font-size: 11dp;
}

.perf-hud-tracking-off.hidden {
    display: none;
}

.perf-hud-enable-tracking {
    height: 24dp;
    padding: 2dp 9dp;
    border-width: 1dp;
    border-radius: 4dp;
    font-size: 11dp;
    cursor: pointer;
}

.perf-hud-ledger-header {
    display: flex;
    flex-direction: column;
    gap: 3dp;
    padding: 5dp 7dp;
    border-width: 1dp;
    border-radius: 5dp;
}

.perf-hud-ledger-header-line {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 8dp;
}

.perf-hud-ledger-header-detail {
    justify-content: flex-start;
    gap: 14dp;
}

.perf-hud-ledger-process {
    font-size: 12dp;
    font-weight: bold;
}

.perf-hud-ledger-header-detail span {
    font-size: 10dp;
}

.perf-hud-ledger-closure {
    padding: 1dp 5dp;
    border-width: 1dp;
    border-radius: 8dp;
    font-size: 10dp;
}

.perf-hud-ledger-over-banner {
    padding: 5dp 8dp;
    border-width: 1dp;
    border-radius: 4dp;
    font-size: 11dp;
    font-weight: bold;
    text-align: center;
}

.perf-hud-ledger-over-banner.hidden {
    display: none;
}

.perf-hud-ledger-rows {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 1dp;
    overflow-y: auto;
}

.perf-hud-ledger-row {
    display: flex;
    align-items: center;
    gap: 6dp;
    min-height: 30dp;
    padding: 2dp 3dp;
    border-radius: 3dp;
}

.perf-hud-ledger-row.root {
    margin-top: 2dp;
    border-top-width: 1dp;
}

.perf-hud-ledger-row.has-children {
    cursor: pointer;
}

.perf-hud-ledger-row-header {
    min-height: 18dp;
    max-height: 18dp;
    font-size: 9dp;
    font-weight: bold;
}

.perf-hud-ledger-name-cell {
    flex: 1;
    min-width: 80dp;
    display: flex;
    align-items: baseline;
    gap: 4dp;
    overflow: hidden;
}

.perf-hud-ledger-toggle {
    width: 11dp;
    min-width: 11dp;
    text-align: center;
    font-size: 10dp;
}

.perf-hud-ledger-name {
    font-size: 11dp;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.perf-hud-ledger-note {
    flex: 1;
    min-width: 0;
    font-size: 9dp;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.perf-hud-ledger-share {
    width: 64dp;
    min-width: 64dp;
    height: 5dp;
    border-radius: 3dp;
    overflow: hidden;
}

.perf-hud-ledger-share-fill {
    display: block;
    height: 5dp;
    border-radius: 3dp;
}

.perf-hud-ledger-disclosure {
    width: 100dp;
    min-width: 100dp;
    flex-shrink: 1;
    font-size: 9dp;
    text-align: right;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.perf-hud-ledger-allocated {
    width: 76dp;
    min-width: 76dp;
    flex-shrink: 0;
    font-size: 10dp;
    text-align: right;
}

.perf-hud-ledger-badge {
    width: 28dp;
    min-width: 28dp;
    flex-shrink: 0;
    padding: 1dp 2dp;
    border-width: 1dp;
    border-radius: 7dp;
    font-size: 9dp;
    text-align: center;
}

.vram-hud-counters-empty {
    font-size: 11dp;
}

.vram-hud-allocs-summary {
    display: flex;
    justify-content: space-between;
    padding: 2dp 3dp;
    font-size: 11dp;
    font-weight: bold;
}

.vram-hud-allocs-rows {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 1dp;
}

.vram-hud-allocs-row {
    display: flex;
    gap: 8dp;
    align-items: baseline;
    padding: 1dp 3dp;
}

.vram-hud-allocs-name {
    flex: 1;
    font-size: 11dp;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.vram-hud-allocs-bytes {
    font-size: 11dp;
    font-weight: bold;
    width: 90dp;
    text-align: right;
}

.vram-hud-allocs-pct {
    font-size: 11dp;
    width: 52dp;
    text-align: right;
}

.vram-hud-anno-summary {
    display: flex;
    justify-content: space-between;
    padding: 2dp 3dp;
    font-size: 11dp;
    font-weight: bold;
}

.vram-hud-anno-row {
    display: flex;
    gap: 6dp;
    align-items: baseline;
    padding: 1dp 3dp;
}

.vram-hud-anno-row-header {
    height: 18dp;
    min-height: 18dp;
    max-height: 18dp;
    font-weight: bold;
}

.vram-hud-anno-row-header span {
    font-size: 10dp;
}

.vram-hud-anno-cat {
    width: 32dp;
    font-size: 10dp;
    text-align: center;
}

.vram-hud-anno-name {
    flex: 1;
    font-size: 11dp;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.vram-hud-anno-bytes,
.vram-hud-anno-peak {
    width: 72dp;
    font-size: 11dp;
    font-weight: bold;
    text-align: right;
}

.vram-hud-anno-wall,
.vram-hud-anno-gpu {
    width: 60dp;
    font-size: 11dp;
    text-align: right;
}

.vram-hud-anno-calls {
    width: 56dp;
    font-size: 11dp;
    text-align: right;
}

.vram-hud-anno-rows {
    flex: 1;
    min-height: 0;
    display: flex;
    flex-direction: column;
    gap: 1dp;
}

.vram-hud-filter-row {
    display: flex;
    align-items: center;
    gap: 4dp;
    padding-bottom: 4dp;
}

#vram-hud-filter {
    flex: 1;
    height: 22dp;
    font-size: 11dp;
    padding: 2dp 6dp;
    border-width: 1dp;
    border-radius: 4dp;
}

.vram-hud-filter-clear {
    width: 22dp;
    height: 22dp;
    font-size: 10dp;
    text-align: center;
    border-width: 1dp;
    border-radius: 4dp;
    cursor: pointer;
}

.vram-hud-filter-clear.hidden {
    display: none;
}

.vram-hud-resize {
    position: absolute;
    right: 0dp;
    bottom: 0dp;
    width: 14dp;
    height: 14dp;
    drag: drag;
    cursor: nwse-resize;
}

.vram-hud-title {
    font-size: 13dp;
    font-weight: bold;
}

.vram-hud-iteration {
    font-size: 11dp;
}

.vram-hud-summary {
    display: flex;
    flex-direction: column;
    gap: 3dp;
    padding-bottom: 6dp;
}

.vram-hud-summary-row {
    display: flex;
    justify-content: space-between;
    gap: 12dp;
}

.vram-hud-summary-label,
.vram-hud-device,
.vram-hud-empty {
    font-size: 12dp;
}

.vram-hud-summary-value {
    font-size: 12dp;
    font-weight: bold;
}

.vram-hud-device {
    margin-top: 2dp;
}

.vram-hud-rows {
    display: flex;
    flex-direction: column;
    gap: 2dp;
    flex: 1;
    min-height: 0;
    overflow-y: auto;
}

.vram-hud-tree-row {
    display: flex;
    gap: 8dp;
    align-items: baseline;
    height: 28dp;
    min-height: 28dp;
    max-height: 28dp;
    padding: 2dp 3dp;
    overflow: hidden;
}

.vram-hud-tree-row.has-children {
    cursor: pointer;
}

.vram-hud-tree-row span {
    font-size: 10dp;
}

.vram-hud-row-name,
.vram-hud-col-source {
    width: 240dp;
}

.vram-hud-row-name {
    display: flex;
    gap: 4dp;
    align-items: baseline;
    min-width: 0;
}

.vram-hud-col-live {
    width: 78dp;
}

.vram-hud-col-peak {
    width: 78dp;
}

.vram-hud-col-delta {
    width: 82dp;
}

.vram-hud-col-time {
    width: 68dp;
}

.vram-hud-col-gpu {
    width: 68dp;
}

.vram-hud-header-meta {
    display: flex;
    gap: 8dp;
    align-items: baseline;
}

.perf-hud-card-chevron {
    width: 14dp;
    min-width: 14dp;
    font-size: 12dp;
    text-align: center;
    cursor: pointer;
    drag: none;
}

.vram-hud-throughput {
    font-size: 11dp;
}

.vram-hud-throughput.hidden {
    display: none;
}

.vram-hud-counters {
    display: flex;
    flex-direction: column;
    gap: 2dp;
    padding-bottom: 6dp;
}

.vram-hud-counters.hidden {
    display: none;
}

.vram-hud-counter-row {
    display: flex;
    justify-content: space-between;
    gap: 12dp;
}

.vram-hud-counter-label {
    font-size: 12dp;
}

.vram-hud-counter-value {
    font-size: 12dp;
    font-weight: bold;
}

.vram-hud-node-label {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.vram-hud-row-header span {
    font-size: 10dp;
    font-weight: bold;
}

.vram-hud-row-header {
    height: 18dp;
    min-height: 18dp;
    max-height: 18dp;
}

.vram-hud-tree-row em {
    display: block;
    font-style: normal;
    font-size: 9dp;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.vram-hud-expand-toggle {
    display: inline-block;
    width: 12dp;
    font-size: 12dp;
    text-align: center;
    cursor: pointer;
}

.vram-hud-row-badges {
    display: inline;
    font-size: 9dp;
}

.vram-hud-empty {
    padding: 4dp 6dp;
    font-size: 11dp;
}

.hidden {
    display: none;
}

.perf-hud-strip {
    position: absolute;
    right: 18dp;
    top: 18dp;
    width: 300dp;
    min-height: 142dp;
    padding: 10dp 12dp;
    display: flex;
    flex-direction: column;
    gap: 5dp;
    border-width: 1dp;
    border-radius: 7dp;
    z-index: 30;
}

.perf-hud-strip-header {
    display: flex;
    align-items: center;
    gap: 6dp;
    height: 18dp;
    cursor: pointer;
}

.perf-hud-strip-title { flex-grow: 1; }
.perf-hud-strip-rate, .perf-hud-strip-value, .perf-hud-strip-badge { font-family: "JetBrains Mono"; font-weight: normal; }
.perf-hud-strip-meter { display: flex; align-items: center; gap: 5dp; height: 18dp; cursor: pointer; }
.perf-hud-strip-label { width: 34dp; }
.perf-hud-meter-track { flex-grow: 1; display: flex; height: 7dp; overflow: hidden; border-radius: 3dp; }
.perf-hud-meter-fill { display: block; height: 100%; width: 0%; }
/* Fits "23.51 GiB / 23.51 GiB" on one line (mono 10dp). */
.perf-hud-strip-value { width: 128dp; min-width: 128dp; text-align: right; font-size: 10dp; white-space: nowrap; }
.perf-hud-strip-badge { width: 12dp; font-size: 10dp; }
.perf-hud-core-strip { display: flex; align-items: flex-end; gap: 1dp; height: 13dp; margin-left: 39dp; overflow: hidden; }
.perf-hud-core-strip .perf-hud-core-bar { flex: 1 1 0; min-width: 1dp; max-width: 4dp; height: 100%; }
.perf-hud-strip-header, .perf-hud-strip-meter, .perf-hud-core-strip { box-sizing: border-box; }
.vram-hud-overlay.perf-hud-compact .vram-hud-card { display: none; }
.vram-hud-overlay.perf-hud-compact .perf-hud-strip { display: flex; }
.vram-hud-overlay:not(.perf-hud-compact) .perf-hud-strip { display: none; }
.perf-hud-strip.hidden { display: none; }

.perf-hud-overview-meters {
    display: flex;
    flex-direction: column;
    gap: 6dp;
    margin-top: 8dp;
}

.perf-hud-sparkline-row {
    display: flex;
    align-items: center;
    gap: 6dp;
    height: 28dp;
}

.perf-hud-sparkline-label {
    width: 34dp;
    font-size: 10dp;
}

.perf-hud-sparkline {
    flex-grow: 1;
    height: 24dp;
    display: flex;
    align-items: flex-end;
    gap: 1dp;
    overflow: hidden;
}

.perf-hud-sparkline .histogram-bar-fill {
    flex: 1 1 0;
    min-width: 1dp;
    max-width: 4dp;
    height: 0%;
}

.vram-hud-timeline-toolbar, .vram-hud-timeline-legend {
    display: flex;
    flex-wrap: wrap;
    gap: 4dp;
    margin-bottom: 5dp;
}

.vram-hud-timeline-toolbar button, .vram-hud-timeline-legend button {
    font-size: 10dp;
    padding: 3dp 6dp;
    cursor: pointer;
}

.vram-hud-timeline { display: block; width: 100%; height: 100%; }
.vram-hud-timeline-frame { position: relative; width: 100%; height: 48%; min-height: 220dp; flex-shrink: 0; margin-bottom: 0dp; overflow: hidden; }
.vram-hud-timeline-axis { position: absolute; top: 0; bottom: 0; left: 3dp; display: flex; flex-direction: column; justify-content: space-between; pointer-events: none; font-family: "JetBrains Mono"; font-size: 9dp; }
.vram-hud-timeline-capacity { position: absolute; top: 16dp; right: 5dp; pointer-events: none; font-family: "JetBrains Mono"; font-size: 9dp; }
.vram-hud-timeline-x-axis { width: 100%; display: flex; justify-content: space-between; height: 15dp; font-family: "JetBrains Mono"; font-size: 9dp; }
.vram-hud-timeline-markers { position: absolute; top: 0; left: 0; right: 0; height: 16dp; pointer-events: none; }
.vram-hud-marker { position: absolute; top: 0; font-size: 8dp; font-family: "JetBrains Mono"; opacity: .55; pointer-events: auto; }
#vram-hud-panel-overview > * { flex-shrink: 0; }
.vram-hud-crosshair { position: absolute; display: none; top: 0; bottom: 0; width: 1dp; pointer-events: none; }
.vram-hud-crosshair.active { display: block; }
.vram-hud-mini-timeline { display: block; width: 100%; height: 28dp; }
.vram-hud-health { font-size: 10dp; margin-right: 7dp; }
.vram-hud-timeline-tooltip { min-height: 16dp; font-family: "JetBrains Mono"; font-size: 10dp; white-space: normal; }
.vram-hud-timeline-legend button { display: flex; align-items: center; gap: 4dp; }
.vram-hud-timeline-legend button.muted { opacity: .42; }
.vram-hud-legend-swatch { display: inline-block; width: 8dp; height: 8dp; border-radius: 2dp; }
.vram-hud-legend-chip b { font-family: "JetBrains Mono"; font-weight: normal; margin-left: 3dp; }
.vram-hud-section-title { font-size: 11dp; font-weight: bold; cursor: pointer; margin-top: 7dp; }
.vram-hud-peak { display: flex; flex-direction: column; gap: 3dp; }
.vram-hud-peak-bar { display: flex; width: 100%; height: 11dp; overflow: hidden; }
.vram-hud-peak-segment { height: 100%; }
.vram-hud-peak-row { display: flex; justify-content: space-between; align-items: center; gap: 6dp; font-size: 10dp; }
.vram-hud-peak-value { min-width: 155dp; text-align: right; font-family: "JetBrains Mono"; }
.vram-hud-movers { max-height: 130dp; overflow-y: auto; font-family: "JetBrains Mono"; font-size: 10dp; }
.vram-hud-mover-row { display: flex; align-items: center; gap: 6dp; padding: 2dp; cursor: pointer; }
.vram-hud-mover-name { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.vram-hud-mover-number { width: 76dp; min-width: 76dp; text-align: right; }
.vram-hud-mover-spark { display: flex; align-items: flex-end; width: 62dp; min-width: 62dp; height: 14dp; gap: 1dp; }
.vram-hud-mover-spark span { flex: 1 1 0; min-width: 1dp; }
.vram-hud-export-path { font-size: 10dp; margin-top: 4dp; }
.vram-hud-overlay .vram-hud-mini-timeline { display: none; }
.vram-hud-overlay.perf-hud-compact .vram-hud-mini-timeline { display: block; }

.perf-hud-ledger-allocated,
.perf-hud-ledger-disclosure,
.perf-hud-ledger-badge,
.vram-hud-summary-value,
.vram-hud-col-live,
.vram-hud-col-peak,
.vram-hud-col-delta,
.vram-hud-col-time,
.vram-hud-col-gpu,
.vram-hud-anno-bytes,
.vram-hud-anno-peak,
.vram-hud-anno-wall,
.vram-hud-anno-gpu,
.vram-hud-anno-calls,
.vram-hud-allocs-summary-value,
.vram-hud-breakdown-bytes,
.vram-hud-breakdown-pct {
    font-family: "JetBrains Mono";
    font-weight: normal;
}

/* Leave room for the tool strip and corner axis control in each pane. */
.toolbar-hcenter.align-active {
    padding-left: 44dp;
    padding-right: 90dp;
    box-sizing: border-box;
}

/* Keep labeled alignment options inside narrow viewport panes. */
.toolbar-hcenter .align-toolbar {
    max-width: 100%;
    flex-wrap: wrap;
    justify-content: center;
}

/* Alignment options need names as well as icons. */
.toolbar-hcenter .align-toolbar .icon-btn.has-label {
    width: auto;
    padding-left: 7dp;
    padding-right: 7dp;
    gap: 5dp;
}

.align-button-label {
    white-space: nowrap;
    font-size: 13dp;
}
